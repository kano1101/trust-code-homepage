# /etc/nginx/conf.d/default.conf
server {
  listen 80;
  server_name _;

  # WordPress 本体（Apache）へプロキシ
  location / {
    proxy_pass         http://wordpress:80;
    proxy_set_header   Host $host;
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_read_timeout 300;
  }

  # アップロード最大サイズ（必要に応じて調整）
  client_max_body_size 128m;

  # 静的ファイルはキャッシュ
  location ~* \.(?:css|js|jpg|jpeg|png|gif|svg|webp|ico|ttf|otf|woff|woff2)$ {
    proxy_pass http://wordpress:80;
    expires 7d;
    access_log off;
  }
}

# phpMyAdmin を別ホスト名で出したい場合は Cloudflare 側でホスト名を分け、
# そのホスト名に対して下記のような server を追加し proxy_pass http://wp-pma:80; にする。
# server { listen 80; server_name pma.example.com; location / { proxy_pass http://wp-pma:80; ... } }